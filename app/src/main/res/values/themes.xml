<resources>
    <!-- Base application theme. -->
    <style name="Theme.JobFinder" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorSecondaryVariant">@color/secondary_dark</item>
        <item name="colorOnSecondary">@color/dark</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorOnPrimary</item>
        <item name="android:windowLightStatusBar">true</item>
        <!-- Customize your theme here. -->
        <item name="android:background">@color/white</item>
        <item name="android:windowBackground">@color/modal_light</item>

        <item name="android:textColorPrimary">@color/text</item>
        <item name="android:textColorSecondary">@color/placeholder_text</item>
        <item name="android:textColorHint">@color/placeholder_text</item>

        <item name="bottomSheetDialogTheme">@style/Theme.Modal</item>
    </style>

    <style name="Theme.Modal" parent="ThemeOverlay.MaterialComponents.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/Theme.BottomSheet.Modal</item>
        <item name="materialButtonStyle">@style/Theme.BottomSheet.Button</item>
        <item name="android:background">?android:windowBackground</item>
    </style>

    <style name="Theme.BottomSheet.Button" parent="Widget.MaterialComponents.Button">
        <item name="background">@color/primary</item>
    </style>

    <style name="Theme.BottomSheet.Modal" parent="Widget.MaterialComponents.BottomSheet.Modal">
        <item name="backgroundTint">@color/white</item>
    </style>
</resources>